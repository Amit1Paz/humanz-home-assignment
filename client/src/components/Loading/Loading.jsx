import { StyledLoading } from "./Loading.styled";
import LoadingImg from "../../assets/images/loading.svg";

const Loading = () => {
  return <StyledLoading src={LoadingImg} alt="Loading" />;
};

export default Loading;
